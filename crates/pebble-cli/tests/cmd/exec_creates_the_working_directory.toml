bin.name = "pebble"
args = ["exec", "--model", "openai/gpt-5.4", "--quiet", "--cwd", "fresh/dir", "say hello"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec_creates_the_working_directory"
