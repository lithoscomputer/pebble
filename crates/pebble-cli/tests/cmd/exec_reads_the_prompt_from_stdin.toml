bin.name = "pebble"
args = ["exec", "--quiet", "--model", "openai/gpt-5.4"]
stdin = "say hello from stdin"
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec-reads-the-prompt-from-stdin"
