bin.name = "pebble"
args = ["exec", "--model", "moonshot/kimi-k3", "--permission", "full", "--verbose", "check the notes"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
MOONSHOT_API_KEY = "exec_verbose_shows_everything"
