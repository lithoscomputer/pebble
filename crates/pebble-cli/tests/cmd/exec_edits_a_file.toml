bin.name = "pebble"
args = ["exec", "--model", "venice/deepseek-v4-flash", "--quiet", "fix the typo"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
VENICE_API_KEY = "exec_edits_a_file"
