bin.name = "pebble"
args = ["exec", "--model", "openai/gpt-5.4", "--quiet", "say hello"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec_replays_a_malformed_stream"
