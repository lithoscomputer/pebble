bin.name = "pebble"
args = ["exec", "--model", "venice/deepseek-v4-flash", "--quiet", "say hello"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
VENICE_API_KEY = "exec_answers_through_venice"
