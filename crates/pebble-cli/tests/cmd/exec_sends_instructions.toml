bin.name = "pebble"
args = ["exec", "--model", "openai/gpt-5.4", "--quiet", "--instructions", "Always answer in French.", "say hello"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec_sends_instructions"
