bin.name = "pebble"
args = ["exec", "--model", "openai/gpt-5.4", "--quiet", "--subagents", "delegate the write"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec_subagent_uses_inherited_tools"
