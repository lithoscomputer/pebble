bin.name = "pebble"
args = ["exec", "--model", "openai/gpt-5.4", "--permission", "full", "run the build"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec_runs_a_command"
