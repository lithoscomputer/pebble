bin.name = "pebble"
args = ["exec", "--model", "openai/gpt-5.4", "--json", "write it"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec_streams_json_events"
