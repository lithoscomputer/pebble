bin.name = "pebble"
args = ["exec", "--model", "openai/gpt-5.4", "--quiet", "--subagents", "delegate twice"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec_spawns_two_subagents"
