bin.name = "pebble"
args = ["exec", "--model", "openai/gpt-5.4", "--quiet", "find the todo"]
fs.sandbox = true
status = "success"

[env]
inherit = false

[env.add]
OPENAI_API_KEY = "exec_searches_the_tree"
